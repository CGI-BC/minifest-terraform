# ==============================================================
# Terraform
# ==============================================================

# Local state files — use a remote backend instead
*.tfstate
*.tfstate.*
*.tfstate.backup

# Plan output files (contain sensitive data — never commit)
*.tfplan

# Variable files with real values — use .example files in git
terraform.tfvars
*.auto.tfvars

# Terraform working directory
**/.terraform/
**/.terraform.lock.hcl

# Crash logs
crash.log
crash.*.log

# Override files (local developer overrides, not for sharing)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# ==============================================================
# Terragrunt
# ==============================================================

# Terragrunt cache (safe to delete — rebuilt on next run)
**/.terragrunt-cache/

# Auto-generated files (Terragrunt writes these from root config)
**/backend.tf
**/provider.tf
**/versions.tf

